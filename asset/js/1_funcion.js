//funcion inicial
function example(a, b, c) {
  return a + b + c;
}

//definicion funcion de expresion
const example = (a, b, c) => {
  return a + b + c;
};

//llamado
const resultado = example(2, 3, 4);
